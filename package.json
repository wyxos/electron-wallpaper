{
  "name": "@wyxos/electron-wallpaper",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "gypfile": true,
  "scripts": {
    "lint": "eslint lib example && cpplint --linelength=120 --recursive src",
    "example": "electron ./example",
    "configure": "node-gyp configure",
    "build": "node-gyp rebuild",
    "rebuild": "node-gyp rebuild",
    "rebuild-electron": "electron-rebuild -f",
    "install": "prebuild-install || node-gyp rebuild",
    "prebuild-release": "prebuild -t 11.0.0 -t 12.0.9 -r electron --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "11.0.0",
    "electron-rebuild": "^2.3.5",
    "prebuild": "^10.0.1"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^3.2.1",
    "node-gyp": "^8.0.0"
  }
}
